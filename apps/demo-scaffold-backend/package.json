{
  "name": "@ynaa/demo-scaffold-backend",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "tags": [
      "scope:demo-scaffold-backend",
      "type:app"
    ],
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "webpack-cli build",
          "args": [
            "--node-env=production"
          ]
        },
        "configurations": {
          "development": {
            "args": [
              "--node-env=development"
            ]
          }
        }
      },
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@ynaa/demo-scaffold-backend:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@ynaa/demo-scaffold-backend:build:development"
          },
          "production": {
            "buildTarget": "@ynaa/demo-scaffold-backend:build:production"
          }
        }
      },
      "test": {
        "options": {
          "passWithNoTests": true
        }
      }
    }
  }
}
