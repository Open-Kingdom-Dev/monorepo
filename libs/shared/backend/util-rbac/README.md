# util-rbac

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-rbac` to build the library.

## Running unit tests

Run `nx test util-rbac` to execute the unit tests via [Jest](https://jestjs.io).
