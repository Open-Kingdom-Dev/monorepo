# util-crm-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build util-crm-domain` to build the library.

## Running unit tests

Run `nx test util-crm-domain` to execute the unit tests via [Jest](https://jestjs.io).
